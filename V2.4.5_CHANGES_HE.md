# V2.4.5 – WhatsApp phone normalization

- מספר ישראלי מקומי כגון `05XXXXXXXX` מומר אוטומטית ל-`9725XXXXXXXX`.
- נתמכים גם `+972`, `00972`, `9720...` ומספר נייד ישראלי ללא 0.
- לפני שליחה מתבצעת בדיקת פורמט.
- תשובת Meta נשמרת בלוג עם מזהה ההודעה (ללא token), כדי להבדיל בין קבלה לשליחה לבין מסירה בפועל.
- הודעת הבדיקה בממשק מבהירה ש-API accepted אינו אישור מסירה לטלפון.
